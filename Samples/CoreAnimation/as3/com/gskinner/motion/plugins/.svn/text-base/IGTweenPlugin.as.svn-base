package com.gskinner.motion.plugins {
	import com.gskinner.motion.GTween;
	public interface IGTweenPlugin {
		function init(tween : com.gskinner.motion.GTween,name : String,value : Number) : Number ;
		function tween(tween : com.gskinner.motion.GTween,name : String,value : Number,initValue : Number,rangeValue : Number,ratio : Number,end : Boolean) : Number ;
	}
}
