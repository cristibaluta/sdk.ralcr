package caurina.transitions {
	import flash.Boot;
	public class SpecialPropertySplitter {
		public function SpecialPropertySplitter(p_splitFunction : * = null,p_parameters : Array = null) : void { if( !flash.Boot.skip_constructor ) {
			this.splitValues = p_splitFunction;
			this.parameters = p_parameters;
		}}
		
		public var parameters : Array;
		public var splitValues : *;
		public function toString() : String {
			return "[SpecialPropertySplitter " + "splitValues:" + Std.string(this.splitValues) + ", parameters:" + Std.string(this.parameters) + "]";
		}
		
	}
}
